- flash.each do |name, msg|
  - if msg.is_a?(String)
    div.alert-box class="#{name.to_s == 'notice' ? 'success' : 'alert'}" data-alert=""
      = content_tag :div, msg, id: "flash_#{name}", class: 'text-center'
      a.close href="#" &times;